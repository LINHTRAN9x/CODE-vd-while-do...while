#include <stdio.h>
int main(){
    int num; //mặc định num = 0

    while(num <= 5){      // Nếu num nhỏ hơn or bằng 5,thì in ra màn hình theo thứ tự tăng dần.
        printf("%d\n",num);
        num++; // num +1
    }
return 0;
}